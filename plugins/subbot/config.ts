// ======================================================================
//                    plugins/subbot/config.ts
//       子机器人系统全局配置（按钮 / 限制 / 模板）
// ======================================================================


// ==============================================================
//                  子机器人默认按钮模板
// ==============================================================

export const DEFAULT_SUBBOT_MENU = [
  [
    { text: "🎮 功能一", value: "feature_1" },
    { text: "🎯 功能二", value: "feature_2" }
  ],
  [
    { text: "📊 数据统计", value: "stats" }
  ]
];


// ==============================================================
//                  子机器人系统限制（免费）
// ==============================================================

export const SUBBOT_LIMITS = {
  free: {
    broadcast_per_day: 3,  // 免费用户每天广播3次
    listen_limit: 10,      // 免费监听限制
  },
  vip: {
    broadcast_per_day: Infinity,
    listen_limit: Infinity,
  }
};


// ==============================================================
//                  子机器人广播模板（可扩展）
// ==============================================================

export const BROADCAST_TEMPLATE = {
  text_only: (content: string) =>
    `📢 <b>广播消息</b>\n\n${content}`,

  with_footer: (content: string) =>
    `${content}\n\n—— 来自你的子机器人`,
};


// ==============================================================
//                  子机器人默认统计结构
// ==============================================================

export const DEFAULT_SUBBOT_STATS = {
  total_users: 0,
  today_active: 0,
  messages: 0,
};


// ==============================================================
//                 可扩展字段（未来使用）
// ==============================================================

export const SUBBOT_FEATURE_FLAGS = {
  enable_auto_reply: false,
  enable_filter_words: false,
  enable_custom_keyboard: true,
  enable_message_forward: false,
  enable_gpt_ai: false,  // 将来子机器人可使用AI
};


// ==============================================================
//               获取某个子机器人的初始化配置
// ==============================================================

export function createSubbotConfig() {
  return {
    menu: DEFAULT_SUBBOT_MENU,
    limits: SUBBOT_LIMITS.free,
    stats: { ...DEFAULT_SUBBOT_STATS },
    features: { ...SUBBOT_FEATURE_FLAGS },
    broadcast_history: [],
  };
}

